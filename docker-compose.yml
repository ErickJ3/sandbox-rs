services:
  sandbox-ctl:
    build:
      context: .
      dockerfile: Dockerfile
    image: sandbox-rs:latest
    container_name: sandbox-ctl

    # No privileged mode needed — sandbox-rs works unprivileged.
    # For privileged mode (cgroups, chroot), uncomment:
    # privileged: true
    # cgroup: host
    # volumes:
    #   - /sys/fs/cgroup:/sys/fs/cgroup:rw

    # Override command to run a specific sandbox
    # command: run --id test --memory 256M --timeout 30 /bin/echo "Hello from Docker!"

    stdin_open: true
    tty: true
